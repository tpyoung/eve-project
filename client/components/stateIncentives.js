'use strict';
import React from 'react';

const stateIncentives = React.createClass({
  render() {
    if (this.props.vehicle.power )

    return (

    )
  }
});

export default stateIncentives;

//array with gray picture links
//array with color picture links

